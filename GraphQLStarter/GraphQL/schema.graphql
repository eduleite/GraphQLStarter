type Aluno {
	id: ID!
	nome: String!
	idade: Int!
}

type Curso {
	id: ID!
	nome: String!
	descricao: String!
}


type Query {
	alunos: [Aluno!]!
	aluno(id: ID!): Aluno
	cursos: [Curso!]!
}

type Mutation {
	cadastrarAluno(id: ID!, nome: String!, idade: Int!): Boolean!
}